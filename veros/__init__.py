"""Veros — smart contract security scan pipeline.

The modules keep importing each other by plain name off a sys.path entry for
this directory (see each module's header), which is what lets them stay
runnable as loose scripts as well as via the installed `veros` command.
"""
__version__ = "0.1.0"
